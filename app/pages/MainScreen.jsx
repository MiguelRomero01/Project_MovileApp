
import { Button, SafeAreaView, Text, StyleSheet} from "react-native"

const MainScreen = () => {

     return (
          <SafeAreaView>
               <Text>hola Home</Text>
          </SafeAreaView>
        );
};

export default MainScreen